{
  "/home/cole/Projects/warlock/crates/warlock-engine/src": {
    "agent.rs": "Agent trait (run, context_tokens, DEFAULT_CONTEXT_TOKENS) plus Request, File (present/omitted/elided/summarised), ChildDocument, Response, and Error for driving and reporting a single pass.",
    "briefs.rs": "briefs.rs: reads briefs.toml beside the manifest via briefs_path/load_briefs; DEFAULT_BRIEF_DIRECTORY \"docs\"; Briefs struct, check_relative rejects absolute or parent-component directories; Error variants for missing/bad file.",
    "claude_md.rs": "write_claude_md(root) writes CLAUDE.md's warlock:begin/end block via splice(existing) and section(); Written{path()} and Error report the outcome without guiding what the document says.",
    "clock.rs": "Dependency-free clock: now_rfc3339() formats a Unix-seconds instant as RFC3339 via civil_from_days(), clamping to MIN_REPRESENTABLE/MAX_REPRESENTABLE instead of mis-spelling absurd dates.",
    "decide.rs": "decide_state(entry, computed_hash) -> NodeState: compares a PactEntry's recorded hash against computed_hash to settle the four freshness cases; tests cover unpacted nodes, exact grant matches, and hand-written manifest grants.",
    "document.rs": "Fill/Lookup/Described schema, parse/check/render pass: Fill, Expected, Defect, accept, mend, render build the WARLOCK.md document from a Fill; ENTRY_CHARS, PURPOSE_CHARS, LIST_CAP set the caps.",
    "fitting.rs": "Builds the pass request: Fitted, fit walks and budgets files by REQUEST_BYTE_CAP/PER_FILE_BYTE_CAP, eliding or sampling over-cap files (trim_to_budget, lift_from_the_cliff, sample_of), gather_request, Gathered, Problem, Omission, carry_hash.",
    "hash.rs": "subtree_hash(dir) computes a directory's content hash under HASH_CONTEXT, honoring .warlock and ignore rules via files_under; length() and Error round out the module.",
    "ignores.rs": "`.warlockignore` handling: constant FILENAME, and is_ignored(path) which walks one directory deep via walk_one_deep to decide if a path should be skipped.",
    "languages.rs": "Per-language table (`Language`, `Block`, `TABLE`) driving `declared_names`, `skeleton` and `elide`, which strip a file to its declarations/test blocks while keeping every kept line traceable to the original.",
    "lib.rs": "Crate root; content elided, so only file presence and size are known\u2014open lib.rs directly to see module declarations and re-exports.",
    "load.rs": "load.rs: walks the repo into a Tree via load_tree, finding the git root (repository_root), the .warlock manifest and WARLOCK.md, building each Directory/Node's NodeState and scope, and reporting Problem/Error causes.",
    "manifest.rs": "Manifest and PactEntry: the pacts.toml (.warlock dir) reader/writer \u2014 Manifest::load/save/to_toml_string, PactEntry grant/scope/carry_hash fields, manifest_path/to_manifest_path/from_manifest_path, Error variants.",
    "pact.rs": "Pacts a directory subtree into WARLOCK.md: pact_directory, pact_subtree, refresh_subtree, unpact_subtree, closed_scopes_at_or_below, view_file/Viewed, Observer/Pacting, Pacted/Repaired/Refusal/Error/Failure.",
    "scope.rs": "scope.rs \u2014 scope/sigil syntax and coverage: MAXIMUM_CHARACTERS, RULES, Rule, validate_scope, validate_sigil, scope_covering, valid_scope, scope_opens_to (wildcard \"*\" opens every scope).",
    "sigils.rs": "Machine-side sigils store: derives a per-project directory under `.warlock` (project_directory, sigils_path, PROJECT_CONTEXT) and loads/saves the sigil set in `config.toml` (load_sigils, save_sigils, readable_name, Error).",
    "state.rs": "state.rs: NodeState enum (Unpacted, PactedStale, PactedFresh), its ALL constant and is_pacted method; open for how node freshness states are defined and tested.",
    "tree.rs": "Node/Tree model: Node (path, document via IntoDocument, NodeState, children, files, ignored, scope), Tree with walk() (DepthFirst, parents before children), counts() (StateCounts), and find(path)."
  },
  "/home/cole/Projects/warlock/crates/warlock-tui/src": {
    "account.rs": "Account: the run's ledger, built from Section/Log/Line/Outcome, rendering per-directory rows (thinking/waiting/writing, rejected/repaired) via record, close_section, lines and window; money and size format cost and bytes.",
    "app.rs": "App state and view logic: Row, App, Focus, Chrome, Sigils, RunHeader, PactToggle/PactIntent; selection, scrolling, collapsing, pacting, panel cards and document display.",
    "boundary.rs": "Reach and Verdict decide scope access: verdict() checks a manifest against held sigils, Verdict::message() and closed_scope_message()/blocking_scopes_message() render the refusal or hypothesis-only sentence for a label.",
    "chatting.rs": "Chat<C> drives a turn end-to-end: ask/say start it, spawn_turn/run_turn/apply_turn carry it, /brief, /chat and /write commands switch Mode, Chatting/Asked/TurnEvent and Ending track state, end() closes it out.",
    "check.rs": "check.rs \u2014 the `check` command: reports for a `path` whether its covering `scope` is held by the machine's `sigils`, via `checked`/`Checked` and `opens`; prose and JSON via `object`.",
    "claude.rs": "The claude CLI agent: ClaudeAgent runs one-shot doc passes, ChatAgent holds Converses conversations; Cancel, Activities/Activity, and Reading parse the stream into progress and a final Response.",
    "colour.rs": "colour.rs maps NodeState to Color via colour_for, plus FOCUS_COLOUR and GUIDE_COLOUR constants for the panel's fixed accents.",
    "composer.rs": "Composer: multi-line input field with cursor, wrapping, and scroll window. Composer::new/at/cursor/height/window (ComposerWindow), compose_for(KeyEvent)->Composed (submit/keep/leave), paste_for(text)->Pasted; COMPOSER_MAX_ROWS caps growth.",
    "config.rs": "config.rs: the interactive `configure()` sigil prompt \u2014 reads/writes the held sigils file via `hold`, `held_for`, `sigils_in` (rules in `RULES`), and reports current holdings with `holding`/`Held`/`preamble`.",
    "confirm.rs": "confirm.rs: quit-confirmation state \u2014 Answer (Yes/No), QuitConfirm (open/is_open/highlighted) tracking which side is lit, Answered outcome, and answer_for(key, highlighted) mapping arrows/y/n/enter/esc to it.",
    "descent.rs": "Descent enum (wanted() names the root's target) and descend()/carry_on() driving a Pact/un-pact run over a subtree; tests cover fresh-subtree, unwalkable, and cancelled cases.",
    "editing.rs": "Reads $EDITOR via editor_command, spawns it with run_editor on edit_press, and reconciles state on return through came_back and edit_target.",
    "edits.rs": "Opened boundary checks and unpact/scope_add/scope_remove: opens a directory against Manifest scopes, drops a subtree with unpact, and grants or clears a scope, refusing across a closed boundary.",
    "error.rs": "Error: the engine's error type, with from_problems() turning load::Problem slices into an Option<Self>, one_line() collapsing any message to a single printable line, Display, source(), and From<io::Error>.",
    "fixture.rs": "Test fixture: tree() and tree_after_a_run() build a fixed sample Tree across nested directories and states, with files() listing entries; used by tests asserting depth-first order, nesting, and file placement.",
    "input.rs": "Keyboard and mouse input mapping: Action, Pressed, action_for, press_for, MouseAction, mouse_action for tree/panel key and click/wheel handling; bulk of file is #[test] coverage of bindings.",
    "lib.rs": "Crate root; content elided so specifics are unavailable \u2014 open lib.rs directly for its module declarations, re-exports, and any top-level types or functions.",
    "main.rs": "CLI entry and TUI event loop: Cli/Command/ScopeCommand parsing, main, init/run, and Session<S,P,C> driving draw/point/press/paste/keep_up for the interactive tree screen; heavy #[test] coverage of exit statuses below.",
    "pacting.rs": "Runs and drives a pact: `Pact`, `Work`, `Running`, `CancelGuard`, `PactEvent`, `spawn_pact`, `run_pact`, `pact_press`, `refresh_press`, `blocked_unpact`, `Toggled`, `Refusal`; heavily tested below.",
    "panel.rs": "Panel with Showing (Account/Thread/Document) and Mode; Card<T> tracks scroll/follow per card via the Shown trait (Account, Thread, Vec<Line>); scroll_offset, window, show, refill_document, start_turn, record_turn, end_turn drive it.",
    "prompt.rs": "The scope prompt's field and edit logic: ScopeField (directory/text/rule), ScopePrompt (Open/Closed via is_open, field), Edited, and edit_for(key, field) mapping keystrokes to appends, backspace, submit or refusal.",
    "query.rs": "query.rs: the fresh/pacted listing \u2014 Listing enum, list(), listed(), spelled(), object()/envelope()/write_object() building the JSON reply; COMMAND, DIRECTORIES, state_word() for warlock's own state words.",
    "running.rs": "Drives a run over a boundary: `Progress` narrates each directory (starting, repaired, documented, unchanged, skipped), `Report`/`report` summarise failures, `ran`, `descended`, `pact`, and `refresh` are the entry points.",
    "scoping.rs": "Scope-boundary handling: scope_press/scope_edit/scope_submit and with_scope_on(manifest, module, scope) build the sigil prompt, refuse or open it per directory scope, and write edits back to the manifest; remainder is tests.",
    "session.rs": "session.rs \u2014 session lifecycle: Scope, Watched (start/round/caught_up/off_note), reload_tree, start_watching, closed_scope, sigils_under/sigils_held, load_manifest, load_app; NOT_REFRESHED/NOT_WATCHING notices.",
    "standing.rs": "standing.rs holds Standing, the resolved working_dir/repo_root pair: `here`/`at` to establish it, `target` and `spelled` to convert paths, `manifest`/`home` for pact state, and the FOR_* reason constants for why a repo was wanted.",
    "stubs.rs": "Test-double agents: `Passing` (via `filling`) is a no-op `Agent`/`Wired` stub; `Saying` (via `answering`) is a `Converses`/`Wired` stub returning a fixed answer.",
    "submission.rs": "Classifies a submitted draft into `Submitted` (message or refusal) via `submitted_for`, refusing anything after the three commands /brief, /write, /chat with the single-line `REFUSAL`; `Submitted::refusal` extracts it.",
    "template.rs": "brief_template loads brief-template.md beside the manifest or falls back to DEFAULT_TEMPLATE; missing_sections compares a template's headings against a document to name what's dropped, carried, or absent; Error names the unreadable file and reason.",
    "terminal.rs": "Screen trait (size, draw, suspended, report_mouse) and TerminalGuard implementing it via enter/drop; take_terminal, restore_terminal, and install_panic_hook manage raw-mode setup and teardown.",
    "thread.rs": "Ending, Turn and Thread model a conversation card: Thread::ask/note/record/answer/end build Turns, Turn::rows/window render lines, ending_for maps agent::Error to an Ending.",
    "ui.rs": "Draws the whole TUI frame: draw() lays out tree pane, panel (account/thread cards, run header via draw_run_header, mark via draw_mark), composer, footer keys_line, and modal draw_confirm/draw_scope; hit_test maps points back to rows.",
    "viewing.rs": "view_press(app) reads the selected row on 'v': a file's lines into the panel (capped, cut noted), or names the document/pact-key row for a directory; errors (not text, vanished, unreadable) leave the panel unchanged.",
    "watch.rs": "Filesystem watch policy: NodeSet (accepts) tracks walked dirs, WatchPolicy coalesces events into reloads via QUIET_PERIOD/RELOAD_CEILING/COALESCED_RELOADS; Watching/Watch::start/drain/live drive the debounced reload loop.",
    "wrap.rs": "Wraps a Line into rows for a panel width: Shape/shape() measure it, rows() breaks it under its own marker via continued(), while wrapped() and folded() break plain text at spaces or mid-word using filled(), break_at(), first_character().",
    "writing.rs": "writing.rs: the write flow \u2014 proposed_path, write_opened/write_edit/write_submit driving ScopePrompt, slug_of/next_number naming the brief file, fence unwrapping via unfenced/opening_fence/closes, and the wrote/stale/missing/failure message lines."
  }
}