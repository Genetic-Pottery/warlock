<!-- warlock -->
> Written by a model pass over this directory alone, to be read before its source and to say which source to read. A map, not a specification: check anything you are about to rely on against the files themselves, and where this document and the code disagree, the code is right.

# src

The engine crate's core module tree: loads a repo into a Tree of freshness states, runs the pact/mend cycle against WARLOCK.md documents, and defines the Agent port used to talk to model-running processes.

## Files

- `agent.rs` (11.4 KB) — Defines the Agent trait plus Request/File/ChildDocument/Response/Error types forming the transport-free port between the engine and a model-running process. · declares `Agent`, `Request`, `new`, `with_files`, `with_child_documents`, `with_prompt`, `prompt`, `directory`, `files`, `child_documents`, `File`, `present`, `omitted`, `elided`, `path`, `bytes` (+14)
- `briefs.rs` (5.7 KB) — Loads and validates briefs.toml (directory setting, default "docs"), rejecting absolute or `..` paths; exposes briefs_path() and load_briefs(). · declares `DEFAULT_BRIEF_DIRECTORY`, `briefs_path`, `load_briefs`, `Error`, `BRIEFS_FILE`, `check_relative`, `Briefs`, `default_directory`, `fmt`, `source`
- `claude_md.rs` (13.5 KB) — Writes the warlock section into CLAUDE.md via write_claude_md, splicing between <!-- warlock:begin/end --> markers; defines Written and Error · declares `write_claude_md`, `Written`, `path`, `Error`, `FILE`, `BEGIN`, `END`, `BODY`, `section`, `splice`, `fmt`, `source`
- `clock.rs` (5.0 KB) — Dependency-free UTC timestamp: now_rfc3339() renders SystemTime to a 20-byte 'YYYY-MM-DDTHH:MM:SSZ' via civil_from_days (Hinnant's algorithm), clamped to years 0000-9999. · declares `now_rfc3339`, `SECONDS_PER_DAY`, `DAYS_FROM_SHIFTED_EPOCH_TO_UNIX_EPOCH`, `DAYS_PER_ERA`, `MIN_REPRESENTABLE`, `MAX_REPRESENTABLE`, `rfc3339_from_unix_seconds`, `civil_from_days`
- `decide.rs` (1.8 KB) — Pure, total decide_state(entry: Option<&PactEntry>, computed_hash: &str) -> NodeState rule combining a manifest entry with a hash into Unpacted/PactedFresh/PactedStale; no I/O. · declares `decide_state`
- `document.rs` (61.3 KB) — The document model: Fill, Entry, Expected, Evidence, Defect, Mend/Mended, and the synthesis/file prompts, check, mend and render that build and repair WARLOCK.md. · declares `ENTRY_CHARS`, `ENTRY_MINIMUM`, `PURPOSE_CHARS`, `LIST_CAP`, `DECLARED_SHOWN`, `ATTEMPTS`, `STAMP`, `Fill`, `Entry`, `Described`, `written_anywhere`, `mentions_tool`, `identifiers`, `stub`, `to_json`, `stub_answer` (+70)
- `fitting.rs` (9.3 KB) — Snapshot type reading a directory into a synthesis Request plus Described evidence, and one_file() for building a single-file request under PER_FILE_BYTE_CAP, with Measured, Problem and Omission errors. · declares `PER_FILE_BYTE_CAP`, `Snapshot`, `Measured`, `take`, `directory`, `files`, `synthesis_request`, `accept_synthesis`, `mend`, `render`, `carried_bytes`, `one_file`, `byte_count`, `Problem`, `Omission`, `elided_or_whole` (+5)
- `hash.rs` (9.0 KB) — file_hash, bytes_hash, subtree_hash, line_hash and carry_hash digest per-file bytes, a directory's paths+contents, and the pass's document/line inputs, each with its own blake3 domain-separated context. · declares `file_hash`, `bytes_hash`, `line_hash`, `subtree_hash`, `carry_hash`, `length`, `Error`, `HASH_CONTEXT`, `FILE_CONTEXT`, `LINE_CONTEXT`, `CARRY_HASH_CONTEXT`, `update_section`, `update_prefixed`, `fmt`, `source`
- `ignores.rs` (3.1 KB) — is_ignored(path) checks .warlockignore (FILENAME) against one directory's own name by re-walking its parent one level deep; no rules means erroring, not passing. · declares `FILENAME`, `is_ignored`, `walk_one_deep`
- `languages.rs` (21.1 KB) — Per-language table (TABLE) driving declared_names, elide and without_comments, plus Language/Block/Comments and helpers like without_visibility and first_identifier. · declares `Block`, `Comments`, `Language`, `declared_names`, `without_comments`, `Elided`, `elide`, `SLASHES`, `is_test_file`, `declares`, `VISIBILITY`, `KEYWORDS`, `outside_blocks`, `closes_after`, `without_visibility`, `past_receiver` (+6)
- `lib.rs` (2.1 KB) — Crate root re-exporting the engine's public API — Agent, decide_state, load_tree, Manifest, pact_subtree/refresh_subtree/view_file, scope_covering, load_sigils, NodeState, Tree — across its modules.
- `load.rs` (15.0 KB) — load_tree(working_dir) builds a Tree by walking the repo from .git-anchored repository_root, colouring each Node's NodeState via the pact Manifest and reporting hash/scope Problems in Loaded. · declares `load_tree`, `Loaded`, `Problem`, `ProblemCause`, `repository_root`, `Error`, `GIT_DIR`, `fmt`, `source`, `walk`, `mark_excluded`, `Directory`, `Builder`, `node`, `children_of`, `state_of` (+2)
- `manifest.rs` (21.2 KB) — Manifest and PactEntry: reads/writes pacts.toml (SCHEMA_VERSION 1) with atomic save, path conversion (to_manifest_path/from_manifest_path), grant and per-file `lines` tracking. · declares `ROOT_MODULE`, `SCHEMA_VERSION`, `Manifest`, `new`, `with_entries`, `version`, `entries`, `push`, `entry`, `to_toml_string`, `from_toml_str`, `save`, `load`, `PactEntry`, `with_grant`, `with_carry_hash` (+28)
- `pact.rs` (55.8 KB) — pact.rs: two-phase describe-then-grant engine—pact_subtree, refresh_subtree, pact_directory, unpact_subtree/unpact_ignored, closed_scopes_at_or_below, Observer trait, PactEntry outcomes/grants, Failure/Error types. · declares `pact_subtree`, `refresh_subtree`, `unpact_subtree`, `unpact_ignored`, `closed_scopes_at_or_below`, `pact_directory`, `pactable_directories`, `view_file`, `Observer`, `Pacting`, `Unwatched`, `PactedSubtree`, `Pacted`, `Repaired`, `Viewed`, `Unviewable` (+41)
- `scope.rs` (8.7 KB) — Validates scope and sigil strings via validate_scope/validate_sigil, resolves the nearest covering scope with scope_covering, and tests admission with scope_opens_to. · declares `RULES`, `Rule`, `validate_scope`, `validate_sigil`, `scope_covering`, `valid_scope`, `scope_opens_to`, `MAXIMUM_CHARACTERS`, `WILDCARD`, `fmt`, `at_or_above`, `is_scope_character`, `is_separator`
- `sigils.rs` (7.5 KB) — sigils.rs: per-project sigil storage at ~/.warlock/<project_directory>/config.toml — project_directory, sigils_path, load_sigils, save_sigils, and Error {NotFound, Io, Syntax, Serialize}. · declares `project_directory`, `sigils_path`, `load_sigils`, `save_sigils`, `Error`, `SIGIL_DIR`, `SIGIL_FILE`, `PROJECT_CONTEXT`, `DIGEST_CHARACTERS`, `MAXIMUM_NAME_CHARACTERS`, `UNNAMED_ROOT`, `project_dir`, `readable_name`, `Config`, `fmt`, `source`
- `state.rs` (1.0 KB) — NodeState enum (Unpacted, PactedStale, PactedFresh) with ALL const and is_pacted(); the exhaustive freshness state, no null/Option representable. · declares `NodeState`, `ALL`, `is_pacted`
- `tree.rs` (7.7 KB) — tree.rs — Node, Tree, DepthFirst iterator (walk), StateCounts, and IntoDocument conversions for building and querying the module tree. · declares `Node`, `new`, `with_children`, `with_files`, `with_ignored`, `is_ignored`, `with_scope`, `is_leaf`, `IntoDocument`, `Tree`, `root_path`, `walk`, `counts`, `find`, `DepthFirst`, `StateCounts` (+6)
- `walk.rs` (5.8 KB) — Builds ignore-aware WalkBuilder walkers and derives subtree_files, pactable_directories, and per-directory Own{files, child_documents} for document generation. · declares `DOCUMENT_FILE`, `MANIFEST_DIR`, `listing`, `honouring_ignores`, `subtree_files`, `pactable_directories`, `Own`, `own`, `Error`, `OWN_DEPTH`, `PROSE_EXTENSIONS`, `usable`, `relative`, `is_prose`

## Structure

- Defines the Agent trait plus Request/File/ChildDocument/Response/Error types forming the transport-free port between the engine and a model-running process.
- Loads and validates briefs.toml (directory setting, default "docs"), rejecting absolute or `..` paths; exposes briefs_path() and load_briefs().
- Writes the warlock section into CLAUDE.md via write_claude_md, splicing between <!-- warlock:begin/end --> markers; defines Written and Error
- Dependency-free UTC timestamp: now_rfc3339() renders SystemTime to a 20-byte 'YYYY-MM-DDTHH:MM:SSZ' via civil_from_days (Hinnant's algorithm), clamped to years 0000-9999.
- Pure, total decide_state(entry: Option<&PactEntry>, computed_hash: &str) -> NodeState rule combining a manifest entry with a hash into Unpacted/PactedFresh/PactedStale; no I/O.
- The document model: Fill, Entry, Expected, Evidence, Defect, Mend/Mended, and the synthesis/file prompts, check, mend and render that build and repair WARLOCK.md.
- Snapshot type reading a directory into a synthesis Request plus Described evidence, and one_file() for building a single-file request under PER_FILE_BYTE_CAP, with Measured, Problem and Omission errors.
- file_hash, bytes_hash, subtree_hash, line_hash and carry_hash digest per-file bytes, a directory's paths+contents, and the pass's document/line inputs, each with its own blake3 domain-separated context.
- is_ignored(path) checks .warlockignore (FILENAME) against one directory's own name by re-walking its parent one level deep; no rules means erroring, not passing.
- Per-language table (TABLE) driving declared_names, elide and without_comments, plus Language/Block/Comments and helpers like without_visibility and first_identifier.
- Crate root re-exporting the engine's public API — Agent, decide_state, load_tree, Manifest, pact_subtree/refresh_subtree/view_file, scope_covering, load_sigils, NodeState, Tree — across its modules.
- load_tree(working_dir) builds a Tree by walking the repo from .git-anchored repository_root, colouring each Node's NodeState via the pact Manifest and reporting hash/scope Problems in Loaded.
