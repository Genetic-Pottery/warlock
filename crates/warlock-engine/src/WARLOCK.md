<!-- warlock -->
> Written by a model pass over this directory alone, to be read before its source and to say which source to read. A map, not a specification: check anything you are about to rely on against the files themselves, and where this document and the code disagree, the code is right.

# src

The engine crate root: builds and maintains the freshness ledger of a repo tree, hashing files, tracking pacted/unpacted state, walking directories, and filling WARLOCK.md documents via an agent port.

## Files

- `agent.rs` (24.0 KB) — Defines the `Agent` trait plus `Request`, `File`, `ChildDocument`, `Response` and `Error`, the model port with no subprocess behind it here. · declares `Agent`, `Request`, `new`, `with_files`, `with_child_documents`, `with_prompt`, `prompt`, `directory`, `files`, `child_documents`, `File`, `present`, `omitted`, `elided`, `path`, `bytes` (+14)
- `briefs.rs` (15.7 KB) — Reads briefs.toml via briefs_path/load_briefs, defaulting to DEFAULT_BRIEF_DIRECTORY and rejecting absolute or `..` directory settings. · declares `DEFAULT_BRIEF_DIRECTORY`, `briefs_path`, `load_briefs`, `Error`, `BRIEFS_FILE`, `check_relative`, `Briefs`, `default_directory`, `fmt`, `source`
- `claude_md.rs` (31.3 KB) — Writes CLAUDE.md by splicing a fixed orientation section (write_claude_md, Written, Error) between BEGIN/END markers, preserving surrounding bytes. · declares `write_claude_md`, `Written`, `path`, `Error`, `FILE`, `BEGIN`, `END`, `BODY`, `section`, `splice`, `fmt`, `source`
- `clock.rs` (10.9 KB) — now_rfc3339 turns SystemTime into a fixed 20-byte UTC RFC3339 string via civil_from_days, a closed-form proleptic Gregorian calendar with no date-parsing dependency. · declares `now_rfc3339`, `SECONDS_PER_DAY`, `DAYS_FROM_SHIFTED_EPOCH_TO_UNIX_EPOCH`, `DAYS_PER_ERA`, `MIN_REPRESENTABLE`, `MAX_REPRESENTABLE`, `rfc3339_from_unix_seconds`, `civil_from_days`
- `decide.rs` (6.9 KB) — decide_state(entry, computed_hash) -> NodeState: the pure rule combining a manifest PactEntry with a computed hash into Unpacted, PactedStale or PactedFresh. · declares `decide_state`
- `document.rs` (120.3 KB) — Fill, Expected, Evidence and Described model a directory's line-filling pass: check, mend and render turn a model's JSON answer into a WARLOCK.md. · declares `ENTRY_CHARS`, `ENTRY_MINIMUM`, `PURPOSE_CHARS`, `LIST_CAP`, `DECLARED_SHOWN`, `ATTEMPTS`, `STAMP`, `Fill`, `Entry`, `Described`, `written_anywhere`, `mentions_tool`, `identifiers`, `stub`, `to_json`, `stub_answer` (+64)
- `fitting.rs` (18.8 KB) — Snapshot::take builds a directory's or single file's agent::Request and Described evidence, capping per-file bytes at PER_FILE_BYTE_CAP and reporting Problem/Omission for anything too large or unreadable. · declares `PER_FILE_BYTE_CAP`, `Snapshot`, `Measured`, `take`, `directory`, `files`, `synthesis_request`, `accept_synthesis`, `mend`, `render`, `carried_bytes`, `one_file`, `byte_count`, `Problem`, `Omission`, `work` (+13)
- `hash.rs` (25.4 KB) — Content hashing: file_hash, subtree_hash and carry_hash digest sorted relative paths and bytes via blake3, keyed by HASH_CONTEXT, FILE_CONTEXT and CARRY_HASH_CONTEXT. · declares `file_hash`, `bytes_hash`, `subtree_hash`, `carry_hash`, `length`, `Error`, `HASH_CONTEXT`, `FILE_CONTEXT`, `CARRY_HASH_CONTEXT`, `fmt`, `source`
- `ignores.rs` (3.1 KB) — Defines FILENAME (".warlockignore") and is_ignored(), a one-deep parent walk that answers whether a directory's own name survives its ancestors' gitignore-syntax rules. · declares `FILENAME`, `is_ignored`, `walk_one_deep`
- `languages.rs` (21.1 KB) — Per-language TABLE of extensions, test-file rules and Block openers; declared_names and elide use it to strip test bodies and keep declaration lines for the skeleton. · declares `Block`, `Language`, `declared_names`, `Elided`, `elide`, `is_test_file`, `declares`, `VISIBILITY`, `KEYWORDS`, `outside_blocks`, `without_visibility`, `past_receiver`, `first_identifier`, `language_of`, `marker_for`, `byte_length` (+4)
- `lib.rs` (2.1 KB) — Crate root declaring the engine's modules (agent, briefs, decide, document, fitting, load, manifest, pact, scope, sigils, state, tree, walk) and re-exporting their public types and functions.
- `load.rs` (55.3 KB) — load_tree() walks a repo from a working dir, builds the Tree by consulting Manifest for each node's NodeState and scope, and collects Problem/Error for hash and scope faults. · declares `load_tree`, `Loaded`, `Problem`, `ProblemCause`, `repository_root`, `Error`, `GIT_DIR`, `fmt`, `source`, `walk`, `mark_excluded`, `Directory`, `Builder`, `node`, `children_of`, `state_of` (+2)
- `manifest.rs` (48.5 KB) — Manifest and PactEntry: the pacts.toml schema — version, module/document paths, scope, grant and carry hashes, per-file lines — with atomic save/load and manifest-path conversion. · declares `ROOT_MODULE`, `SCHEMA_VERSION`, `Manifest`, `new`, `with_entries`, `version`, `entries`, `push`, `entry`, `to_toml_string`, `from_toml_str`, `save`, `load`, `PactEntry`, `with_grant`, `with_carry_hash` (+28)
- `pact.rs` (178.3 KB) — pact_subtree, refresh_subtree, pact_directory, unpact_subtree, unpact_ignored and closed_scopes_at_or_below: the two-phase describe-then-grant engine, per-file line reuse via carry hashes, and the Observer, Pacted, Failure and Error types around it. · declares `pact_subtree`, `refresh_subtree`, `unpact_subtree`, `unpact_ignored`, `closed_scopes_at_or_below`, `pact_directory`, `pactable_directories`, `view_file`, `Observer`, `Pacting`, `Unwatched`, `PactedSubtree`, `Pacted`, `Repaired`, `Viewed`, `Unviewable` (+40)
- `scope.rs` (24.5 KB) — Defines Rule, validate_scope and validate_sigil for the scope character rules, plus scope_covering (nearest-ancestor lookup) and scope_opens_to for sigil matching · declares `RULES`, `Rule`, `validate_scope`, `validate_sigil`, `scope_covering`, `valid_scope`, `scope_opens_to`, `MAXIMUM_CHARACTERS`, `WILDCARD`, `fmt`, `valid_scope_on`, `at_or_above`, `is_scope_character`, `is_separator`
- `sigils.rs` (22.0 KB) — Derives a per-checkout sigil config directory from a home and root, with project_directory, sigils_path, load_sigils and save_sigils reading/writing config.toml's sigils list. · declares `project_directory`, `sigils_path`, `load_sigils`, `save_sigils`, `Error`, `SIGIL_DIR`, `SIGIL_FILE`, `PROJECT_CONTEXT`, `DIGEST_CHARACTERS`, `MAXIMUM_NAME_CHARACTERS`, `UNNAMED_ROOT`, `project_dir`, `readable_name`, `Config`, `fmt`, `source`
- `state.rs` (2.7 KB) — Defines NodeState — Unpacted, PactedStale, PactedFresh — the three freshness variants, with ALL for exhaustive iteration and is_pacted() to test pacted-ness. · declares `NodeState`, `ALL`, `is_pacted`
- `tree.rs` (22.4 KB) — Node and Tree: the freshness ledger's tree, with NodeState-tallying StateCounts, depth-first DepthFirst walk, and find/walk/counts over Node's path, document, state, scope, ignored, children and files. · declares `Node`, `new`, `with_children`, `with_files`, `with_ignored`, `is_ignored`, `with_scope`, `is_leaf`, `IntoDocument`, `Tree`, `root_path`, `walk`, `counts`, `find`, `DepthFirst`, `StateCounts` (+6)
- `walk.rs` (5.8 KB) — Shared filesystem walkers over `ignore::WalkBuilder`: `listing`, `honouring_ignores`, and the `subtree_files`, `pactable_directories`, `own` queries built on them, plus `DOCUMENT_FILE` and `MANIFEST_DIR`. · declares `DOCUMENT_FILE`, `MANIFEST_DIR`, `listing`, `honouring_ignores`, `subtree_files`, `pactable_directories`, `Own`, `own`, `Error`, `OWN_DEPTH`, `PROSE_EXTENSIONS`, `usable`, `relative`, `is_prose`

## Structure

- Defines the `Agent` trait plus `Request`, `File`, `ChildDocument`, `Response` and `Error`, the model port with no subprocess behind it here.
- Reads briefs.toml via briefs_path/load_briefs, defaulting to DEFAULT_BRIEF_DIRECTORY and rejecting absolute or `..` directory settings.
- Writes CLAUDE.md by splicing a fixed orientation section (write_claude_md, Written, Error) between BEGIN/END markers, preserving surrounding bytes.
- now_rfc3339 turns SystemTime into a fixed 20-byte UTC RFC3339 string via civil_from_days, a closed-form proleptic Gregorian calendar with no date-parsing dependency.
- decide_state(entry, computed_hash) -> NodeState: the pure rule combining a manifest PactEntry with a computed hash into Unpacted, PactedStale or PactedFresh.
- Fill, Expected, Evidence and Described model a directory's line-filling pass: check, mend and render turn a model's JSON answer into a WARLOCK.md.
- Snapshot::take builds a directory's or single file's agent::Request and Described evidence, capping per-file bytes at PER_FILE_BYTE_CAP and reporting Problem/Omission for anything too large or unreadable.
- Content hashing: file_hash, subtree_hash and carry_hash digest sorted relative paths and bytes via blake3, keyed by HASH_CONTEXT, FILE_CONTEXT and CARRY_HASH_CONTEXT.
- Defines FILENAME (".warlockignore") and is_ignored(), a one-deep parent walk that answers whether a directory's own name survives its ancestors' gitignore-syntax rules.
- Per-language TABLE of extensions, test-file rules and Block openers; declared_names and elide use it to strip test bodies and keep declaration lines for the skeleton.
- Crate root declaring the engine's modules (agent, briefs, decide, document, fitting, load, manifest, pact, scope, sigils, state, tree, walk) and re-exporting their public types and functions.
- load_tree() walks a repo from a working dir, builds the Tree by consulting Manifest for each node's NodeState and scope, and collects Problem/Error for hash and scope faults.
