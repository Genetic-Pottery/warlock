<!-- warlock -->
> Written by a model pass over this directory alone, to be read before its source and to say which source to read. A map, not a specification: check anything you are about to rely on against the files themselves, and where this document and the code disagree, the code is right.

# src

The engine crate's source: walks a repo into a freshness ledger, hashes and decides pact state per node, and runs the fill-check-mend pipeline to produce and write WARLOCK.md and CLAUDE.md documents under a manifest and scope/sigil boundary model.

## Files

- `agent.rs` (24.0 KB) — Defines the `Agent` trait plus `Request`, `File`, `ChildDocument`, `Response` and `Error`, the model port with no subprocess behind it here. · declares `Agent`, `Request`, `new`, `with_files`, `with_child_documents`, `with_prompt`, `prompt`, `directory`, `files`, `child_documents`, `File`, `present`, `omitted`, `elided`, `path`, `bytes` (+14)
- `briefs.rs` (15.7 KB) — Reads briefs.toml via briefs_path/load_briefs, defaulting to DEFAULT_BRIEF_DIRECTORY and rejecting absolute or `..` directory settings. · declares `DEFAULT_BRIEF_DIRECTORY`, `briefs_path`, `load_briefs`, `Error`, `BRIEFS_FILE`, `check_relative`, `Briefs`, `default_directory`, `fmt`, `source`
- `claude_md.rs` (33.7 KB) — Writes CLAUDE.md via write_claude_md, splicing a fixed warlock:begin/end BODY section explaining WARLOCK.md, colours, and scopes/sigils between markers. · declares `write_claude_md`, `Written`, `path`, `Error`, `FILE`, `BEGIN`, `END`, `BODY`, `section`, `splice`, `fmt`, `source`
- `clock.rs` (10.9 KB) — now_rfc3339 turns SystemTime into a fixed 20-byte UTC RFC3339 string via civil_from_days, a closed-form proleptic Gregorian calendar with no date-parsing dependency. · declares `now_rfc3339`, `SECONDS_PER_DAY`, `DAYS_FROM_SHIFTED_EPOCH_TO_UNIX_EPOCH`, `DAYS_PER_ERA`, `MIN_REPRESENTABLE`, `MAX_REPRESENTABLE`, `rfc3339_from_unix_seconds`, `civil_from_days`
- `decide.rs` (6.9 KB) — decide_state(entry, computed_hash) -> NodeState: the pure rule combining a manifest PactEntry with a computed hash into Unpacted, PactedStale or PactedFresh. · declares `decide_state`
- `document.rs` (119.4 KB) — Defines the Fill/Entry/Described/Evidence/Defect types and the fill-check-mend pipeline (parse, check, mend, render) that turns per-file and synthesis pass answers into a WARLOCK.md document. · declares `ENTRY_CHARS`, `ENTRY_MINIMUM`, `PURPOSE_CHARS`, `LIST_CAP`, `DECLARED_SHOWN`, `ATTEMPTS`, `STAMP`, `Fill`, `Entry`, `Described`, `written_anywhere`, `mentions_tool`, `identifiers`, `stub`, `to_json`, `stub_answer` (+66)
- `fitting.rs` (18.4 KB) — Snapshot::take builds a directory's whole reading (files, child documents, Described) for synthesis; one_file builds a single-file request under PER_FILE_BYTE_CAP, with Problem/Omission naming what was left out and why. · declares `PER_FILE_BYTE_CAP`, `Snapshot`, `Measured`, `take`, `directory`, `files`, `synthesis_request`, `accept_synthesis`, `mend`, `render`, `carried_bytes`, `one_file`, `byte_count`, `Problem`, `Omission`, `work` (+15)
- `hash.rs` (25.1 KB) — Content-addressed hashing: file_hash and subtree_hash (paths+bytes, blake3, domain-separated) plus carry_hash, the early-cutoff digest of a directory's pass inputs. · declares `file_hash`, `bytes_hash`, `subtree_hash`, `carry_hash`, `length`, `Error`, `HASH_CONTEXT`, `FILE_CONTEXT`, `CARRY_HASH_CONTEXT`, `update_section`, `update_prefixed`, `fmt`, `source`
- `ignores.rs` (3.1 KB) — Defines FILENAME (".warlockignore") and is_ignored(), a one-deep parent walk that answers whether a directory's own name survives its ancestors' gitignore-syntax rules. · declares `FILENAME`, `is_ignored`, `walk_one_deep`
- `languages.rs` (20.6 KB) — Per-language table (TABLE, Block, Language) driving declared_names and elide, which strip test bodies and list fn/struct/const-style declarations by extension. · declares `Block`, `Language`, `declared_names`, `Elided`, `elide`, `is_test_file`, `declares`, `VISIBILITY`, `KEYWORDS`, `outside_blocks`, `closes_after`, `without_visibility`, `past_receiver`, `first_identifier`, `language_of`, `byte_length` (+3)
- `lib.rs` (2.1 KB) — Crate root declaring the engine's modules (agent, briefs, decide, document, fitting, load, manifest, pact, scope, sigils, state, tree, walk) and re-exporting their public types and functions.
- `load.rs` (55.1 KB) — load_tree walks the repo from a working dir, finds repository_root via .git, colours each Node's NodeState against the Manifest, and collects Problem/ProblemCause for unreadable or invalid-scope entries. · declares `load_tree`, `Loaded`, `Problem`, `ProblemCause`, `repository_root`, `Error`, `GIT_DIR`, `fmt`, `source`, `walk`, `mark_excluded`, `Directory`, `Builder`, `node`, `children_of`, `state_of` (+2)
- `manifest.rs` (48.5 KB) — Manifest and PactEntry: pacts.toml schema (version, module, document, scope, granted_hash/at, carry_hash, lines), with atomic save/load and manifest-path conversions. · declares `ROOT_MODULE`, `SCHEMA_VERSION`, `Manifest`, `new`, `with_entries`, `version`, `entries`, `push`, `entry`, `to_toml_string`, `from_toml_str`, `save`, `load`, `PactEntry`, `with_grant`, `with_carry_hash` (+28)
- `pact.rs` (174.9 KB) — pact_subtree, refresh_subtree and pact_directory: describes directories bottom-up, grants hashes, writes WARLOCK.md, and un-pacts/scopes entries in Manifest. · declares `pact_subtree`, `refresh_subtree`, `unpact_subtree`, `unpact_ignored`, `closed_scopes_at_or_below`, `pact_directory`, `pactable_directories`, `view_file`, `Observer`, `Pacting`, `Unwatched`, `PactedSubtree`, `Pacted`, `Repaired`, `Viewed`, `Unviewable` (+41)
- `scope.rs` (24.3 KB) — validate_scope/validate_sigil judge scope characters, scope_covering walks manifest ancestors for nearest scope, scope_opens_to tests a held sigil against it · declares `RULES`, `Rule`, `validate_scope`, `validate_sigil`, `scope_covering`, `valid_scope`, `scope_opens_to`, `MAXIMUM_CHARACTERS`, `WILDCARD`, `fmt`, `at_or_above`, `is_scope_character`, `is_separator`
- `sigils.rs` (22.0 KB) — Derives each checkout's `.warlock` sigil directory via `project_directory`, and loads/saves the held sigil set with `load_sigils`, `save_sigils`, `sigils_path` against a caller-given home. · declares `project_directory`, `sigils_path`, `load_sigils`, `save_sigils`, `Error`, `SIGIL_DIR`, `SIGIL_FILE`, `PROJECT_CONTEXT`, `DIGEST_CHARACTERS`, `MAXIMUM_NAME_CHARACTERS`, `UNNAMED_ROOT`, `project_dir`, `readable_name`, `Config`, `fmt`, `source`
- `state.rs` (2.7 KB) — Defines NodeState — Unpacted, PactedStale, PactedFresh — the three freshness variants, with ALL for exhaustive iteration and is_pacted() to test pacted-ness. · declares `NodeState`, `ALL`, `is_pacted`
- `tree.rs` (22.4 KB) — Node and Tree: the freshness ledger's tree, with NodeState-tallying StateCounts, depth-first DepthFirst walk, and find/walk/counts over Node's path, document, state, scope, ignored, children and files. · declares `Node`, `new`, `with_children`, `with_files`, `with_ignored`, `is_ignored`, `with_scope`, `is_leaf`, `IntoDocument`, `Tree`, `root_path`, `walk`, `counts`, `find`, `DepthFirst`, `StateCounts` (+6)
- `walk.rs` (5.8 KB) — Shared filesystem walking: listing/honouring_ignores builders, subtree_files, pactable_directories, and own() collecting a directory's non-prose files and immediate child WARLOCK.md documents. · declares `DOCUMENT_FILE`, `MANIFEST_DIR`, `listing`, `honouring_ignores`, `subtree_files`, `pactable_directories`, `Own`, `own`, `Error`, `OWN_DEPTH`, `PROSE_EXTENSIONS`, `usable`, `relative`, `is_prose`

## Structure

- Defines the `Agent` trait plus `Request`, `File`, `ChildDocument`, `Response` and `Error`, the model port with no subprocess behind it here.
- Reads briefs.toml via briefs_path/load_briefs, defaulting to DEFAULT_BRIEF_DIRECTORY and rejecting absolute or `..` directory settings.
- Writes CLAUDE.md via write_claude_md, splicing a fixed warlock:begin/end BODY section explaining WARLOCK.md, colours, and scopes/sigils between markers.
- now_rfc3339 turns SystemTime into a fixed 20-byte UTC RFC3339 string via civil_from_days, a closed-form proleptic Gregorian calendar with no date-parsing dependency.
- decide_state(entry, computed_hash) -> NodeState: the pure rule combining a manifest PactEntry with a computed hash into Unpacted, PactedStale or PactedFresh.
- Defines the Fill/Entry/Described/Evidence/Defect types and the fill-check-mend pipeline (parse, check, mend, render) that turns per-file and synthesis pass answers into a WARLOCK.md document.
- Snapshot::take builds a directory's whole reading (files, child documents, Described) for synthesis; one_file builds a single-file request under PER_FILE_BYTE_CAP, with Problem/Omission naming what was left out and why.
- Content-addressed hashing: file_hash and subtree_hash (paths+bytes, blake3, domain-separated) plus carry_hash, the early-cutoff digest of a directory's pass inputs.
- Defines FILENAME (".warlockignore") and is_ignored(), a one-deep parent walk that answers whether a directory's own name survives its ancestors' gitignore-syntax rules.
- Per-language table (TABLE, Block, Language) driving declared_names and elide, which strip test bodies and list fn/struct/const-style declarations by extension.
- Crate root declaring the engine's modules (agent, briefs, decide, document, fitting, load, manifest, pact, scope, sigils, state, tree, walk) and re-exporting their public types and functions.
- load_tree walks the repo from a working dir, finds repository_root via .git, colours each Node's NodeState against the Manifest, and collects Problem/ProblemCause for unreadable or invalid-scope entries.
